<div class="container col-md-6 col-md-offset-3">
  <div class="row">
    <div class="panel panel-default col-lg-5 card">
      <div class="panel-body">
        <% @mindfulness.each do |skill| %>
          <%= skill.text %><br>
        <% end %>
      </div>
    </div>
    <div class="panel panel-default col-lg-5 card">
      <div class="panel-body">
        <% @ER.each do |skill| %>
          <%= skill.text %><br>
        <% end %>
      </div>
    </div>
    <div class="panel panel-default col-lg-5 card">
      <div class="panel-body">
        <% @DT.each do |skill| %>
          <%= skill.text %><br>
        <% end %>
      </div>
    </div>
    <div class="panel panel-default col-lg-5 card">
      <div class="panel-body">
        <% @IE.each do |skill| %>
          <%= skill.text %><br>
        <% end %>
      </div>
    </div>
  </div>
</div>